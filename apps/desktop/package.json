{
  "name": "@orbit/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Orbit desktop shell (Tauri) — wraps the static export of apps/web.",
  "scripts": {
    "build:web-export": "BUILD_EXPORT=true npm run build --workspace=apps/web",
    "dev": "tauri dev",
    "build": "npm run build:web-export && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.4"
  }
}
